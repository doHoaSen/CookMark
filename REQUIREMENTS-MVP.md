# Cookmark MVP 요구사항 정의서

## 1. 문서 정보

- 제품: Cookmark
- 대상 버전: MVP
- 기준 문서: `PRD-MVP.md`
- 문서 목적: PRD의 제품 방향을 개발 가능한 요구사항 단위로 정의한다.
- 다음 단계: 본 문서를 바탕으로 화면·API·데이터 동작을 정의하는 기능명세서를 작성한다.

## 2. 제품 목표

Cookmark는 YouTube Shorts, Instagram Reels, 블로그 등에서 발견한 요리 레시피를 저장하고 메뉴별로 관리하며, 실제 요리 경험과 익명 팁을 축적하는 서비스다.

MVP는 다음 핵심 흐름을 검증한다.

```text
레시피 발견
→ URL 저장
→ 메뉴별 분류
→ 레시피 재방문
→ 조리 메모 작성
→ 선택적 익명 팁 공개
```

공개 팁은 실제 조리 여부를 인증한 리뷰가 아니라, 사용자가 레시피에 남기는 경험·의견·변형 팁이다.

## 3. 범위

### 3.1 MVP 포함 범위

- 이메일 기반 회원가입·로그인·로그아웃
- 사용자별 메뉴 관리
- 레시피 URL 저장
- URL 메타데이터 미리보기 및 저장
- 플랫폼 분류
- 메뉴별 레시피 조회
- 레시피 검색과 기본 필터
- 개인 조리 메모
- 선택적 익명 팁 공개
- 공개 팁 수정·공개 취소·삭제
- 공개 팁 신고 및 5회 누적 자동 숨김
- 최소 운영자 화면
- Android 설치형 PWA 공유 수신
- Docker Compose 기반 로컬 개발 환경
- AWS Lightsail 배포를 고려한 실행 구조

### 3.2 MVP 제외 범위

- 공개 피드
- 댓글 간 답글
- 좋아요·도움됨 투표
- 팔로우·사용자 프로필
- 소셜 로그인
- iOS Share Extension
- 네이티브 모바일 앱
- 사진 업로드와 이미지 분석
- 실제 조리 여부를 증명하는 인증
- RAG 자연어 검색
- 자막 기반 재료·조리법 자동 추출
- 장보기 목록과 냉장고 기반 추천

## 4. 사용자 역할

### 4.1 일반 사용자 `USER`

- 자신의 계정으로 로그인할 수 있다.
- 자신의 메뉴·레시피·조리 기록을 관리할 수 있다.
- 레시피에 익명 팁을 작성할 수 있다.
- 자신이 작성한 공개 팁을 수정·공개 취소·삭제할 수 있다.
- 다른 사용자의 공개 팁을 조회·신고할 수 있다.
- 다른 사용자의 개인 데이터에는 접근할 수 없다.

### 4.2 운영자 `ADMIN`

- 신고된 공개 팁을 조회할 수 있다.
- 신고 사유와 신고 횟수를 확인할 수 있다.
- 공개 팁을 숨김·해제·삭제할 수 있다.
- 운영자 기능은 `ADMIN` 권한으로 보호한다.

## 5. 용어 정의

| 용어 | 정의 |
|---|---|
| 레시피 링크 | 사용자가 저장한 원본 URL과 메타데이터 묶음 |
| 메뉴 | 사용자가 레시피를 분류하기 위해 직접 만든 그룹 |
| 조리 기록 | 특정 레시피를 대상으로 작성한 개인 경험 기록 |
| 개인 메모 | 기본적으로 작성자에게만 보이는 조리 기록의 메모 |
| 공개 팁 | 레시피에 연결되어 다른 사용자에게 익명으로 표시되는 내용 |
| 공개 취소 | 공개 팁을 삭제하지 않고 다른 사용자에게서 숨기는 동작 |
| 숨김 | 신고 또는 운영자 처리로 공개 팁을 임시로 표시하지 않는 상태 |

## 6. 기능 요구사항

우선순위는 `MUST`, `SHOULD`, `COULD`로 표시한다.

### 6.1 인증·계정

#### FR-AUTH-001 회원가입 `MUST`

- 사용자는 이메일과 비밀번호로 회원가입할 수 있어야 한다.
- 신규 사용자의 기본 역할은 `USER`여야 한다.
- 이메일은 중복 가입할 수 없어야 한다.
- 비밀번호는 평문으로 저장하지 않아야 한다.

#### FR-AUTH-002 로그인 `MUST`

- 등록된 사용자는 이메일과 비밀번호로 로그인할 수 있어야 한다.
- 로그인 성공 후 인증된 요청을 수행할 수 있어야 한다.
- 인증은 JWT Access Token과 JWT Refresh Token을 사용한다.
- 두 토큰은 HttpOnly Cookie로 전달한다.
- Access Token 만료 시간은 15분이다.
- Refresh Token 만료 시간은 7일이다.
- Refresh Token은 서버에서 해시하여 PostgreSQL에 저장한다.
- Refresh Token 재발급 시 기존 토큰을 폐기하고 새 토큰을 발급한다.
- 로그인 실패 시 민감한 인증 정보를 응답에 포함하지 않아야 한다.

#### FR-AUTH-003 로그아웃 `MUST`

- 사용자는 로그아웃할 수 있어야 한다.
- 로그아웃 후 인증이 필요한 API를 호출할 수 없어야 한다.

#### FR-AUTH-004 역할 기반 접근 제어 `MUST`

- 일반 사용자는 `USER` 역할을 가진다.
- 운영자 기능은 `ADMIN` 역할만 접근할 수 있어야 한다.
- 서버는 요청의 사용자 식별자와 리소스 소유자를 검증해야 한다.

#### FR-AUTH-006 소셜 로그인 확장 구조 `SHOULD`

- MVP에서는 이메일·비밀번호 로그인을 우선 구현한다.
- 사용자 계정과 인증 수단을 분리해 설계한다.
- 향후 Google·Kakao 등 외부 인증 수단을 `AuthIdentity`로 연결할 수 있어야 한다.
- 외부 OAuth 토큰을 내부 API 인증에 직접 사용하지 않고, 로그인 성공 후 Cookmark 자체 JWT를 발급한다.

#### FR-AUTH-005 회원 탈퇴 `SHOULD`

- 회원 탈퇴 시 개인 메뉴·레시피·조리 기록은 삭제 대상이 된다.
- 회원 탈퇴 후 공개 팁은 공개 당시의 내용을 유지한다.
- 공개 팁의 `author_user_id`는 `NULL`로 변경한다.
- 탈퇴한 사용자의 신원은 공개 팁에 표시하지 않는다.

### 6.2 메뉴 관리

#### FR-MENU-001 메뉴 생성 `MUST`

- 사용자는 자신의 메뉴를 생성할 수 있어야 한다.
- 메뉴명은 사용자별로 중복될 수 없어야 한다.

#### FR-MENU-002 메뉴 조회 `MUST`

- 사용자는 자신의 메뉴 목록을 조회할 수 있어야 한다.
- 메뉴별 레시피 수를 확인할 수 있어야 한다.

#### FR-MENU-003 메뉴 수정·삭제 `MUST`

- 사용자는 자신의 메뉴명을 수정할 수 있어야 한다.
- 사용자는 자신의 메뉴를 삭제할 수 있어야 한다.
- 다른 사용자의 메뉴를 수정·삭제할 수 없어야 한다.
- 소속 레시피가 있는 메뉴는 삭제할 수 없어야 한다.
- 사용자는 소속 레시피를 다른 메뉴로 이동한 뒤 메뉴를 삭제해야 한다.

#### FR-MENU-004 메뉴별 레시피 조회 `MUST`

- 사용자는 메뉴 상세에서 해당 메뉴에 속한 자신의 레시피를 조회할 수 있어야 한다.

### 6.3 레시피 링크 저장

#### FR-RECIPE-001 URL 직접 저장 `MUST`

- 로그인 사용자는 URL을 직접 입력해 레시피 링크를 저장할 수 있어야 한다.
- 저장 시 메뉴를 선택하거나 새 메뉴를 생성할 수 있어야 한다.
- 저장 성공 후 저장된 레시피 상세 또는 메뉴 상세로 이동할 수 있어야 한다.

#### FR-RECIPE-002 URL 검증 `MUST`

- `http`, `https` 스킴만 허용한다.
- 잘못된 URL은 저장하지 않고 오류를 반환한다.
- URL 길이와 요청 크기는 서버 제한을 적용한다.

#### FR-RECIPE-003 중복 링크 처리 `MUST`

- 사용자별 정규화 URL을 기준으로 중복을 검사한다.
- 동일 사용자의 동일 링크가 중복 생성되지 않아야 한다.
- 중복 링크 입력 시 기존 레시피를 반환하거나 사용자에게 기존 레시피를 안내한다.

#### FR-RECIPE-004 플랫폼 분류 `MUST`

- URL 호스트를 기준으로 `YOUTUBE`, `INSTAGRAM`, `BLOG`, `OTHER`를 분류한다.
- 분류할 수 없는 URL도 `OTHER`로 저장할 수 있어야 한다.

#### FR-RECIPE-005 메타데이터 미리보기 `MUST`

- 시스템은 Open Graph title, image, description, canonical URL을 추출할 수 있어야 한다.
- 사용자는 저장 전 추출 결과를 확인할 수 있어야 한다.
- 메타데이터 추출 실패 시에도 원본 URL과 사용자가 선택한 메뉴로 저장할 수 있어야 한다.

#### FR-RECIPE-006 메타데이터 요청 보호 `MUST`

- 메타데이터 요청은 서버에서 수행한다.
- 요청 전체 타임아웃은 5초로 한다.
- 최대 응답 본문 크기는 1MB로 한다.
- 리다이렉트는 최대 3회로 제한한다.
- localhost·루프백·사설 IP 등 내부 주소 접근을 차단한다.
- 리다이렉트와 내부망 주소 접근에 대한 제한은 기능명세서에서 구체화한다.

#### FR-RECIPE-007 레시피 조회·수정·삭제 `MUST`

- 사용자는 자신의 레시피 목록과 상세를 조회할 수 있어야 한다.
- 사용자는 자신의 레시피 정보를 수정·삭제할 수 있어야 한다.
- 다른 사용자의 개인 레시피는 조회할 수 없어야 한다.
- 공개 팁은 레시피 소유자의 개인 레시피 삭제와 독립적으로 보존될 수 있어야 한다.

### 6.4 조리 기록과 개인 메모

#### FR-COOK-001 조리 기록 작성 `MUST`

- 사용자는 자신의 레시피에 조리 기록을 작성할 수 있어야 한다.
- 별점은 1~5점 범위로 관리한다.
- 메모, 조리 날짜, 별점은 화면 요구사항에 따라 입력할 수 있어야 한다.
- 사진 업로드는 MVP에서 제공하지 않는다.

#### FR-COOK-002 개인 메모 기본 공개 상태 `MUST`

- 개인 메모의 기본 상태는 `PRIVATE`여야 한다.
- 공개 체크박스의 기본값은 해제여야 한다.
- 체크하지 않고 저장한 개인 메모는 작성자에게만 보여야 한다.

#### FR-COOK-003 메모 공개 전환 `MUST`

- 사용자가 `익명으로 공개하기`를 체크하고 저장하면 공개 팁이 생성되어 즉시 표시되어야 한다.
- 공개 팁에는 작성자 이름, 이메일, 프로필, 사용자 ID를 표시하지 않는다.
- 공개 전 개인정보 입력 주의 문구를 표시한다.

#### FR-COOK-004 개인 메모 조회·수정·삭제 `MUST`

- 사용자는 자신의 조리 기록과 개인 메모를 조회할 수 있어야 한다.
- 사용자는 자신의 조리 기록을 수정·삭제할 수 있어야 한다.
- 다른 사용자의 비공개 조리 기록은 조회할 수 없어야 한다.

### 6.5 익명 공개 팁

#### FR-TIP-001 레시피 상세에서 팁 작성 `MUST`

- 로그인 사용자는 레시피 상세에서 조리 기록 없이도 익명 팁을 작성할 수 있어야 한다.
- 팁은 특정 레시피 링크에 직접 연결되어야 한다.
- 별점은 선택 입력으로 처리한다.
- 실제 조리 여부를 증명하는 자료는 요구하지 않는다.

#### FR-TIP-002 공개 팁 조회 `MUST`

- 사용자는 레시피 상세에서 `PUBLISHED` 상태의 공개 팁을 조회할 수 있어야 한다.
- 공개 팁은 작성자 식별 정보 없이 표시해야 한다.
- 공개 피드나 사용자 프로필을 통해 팁을 탐색하는 기능은 제공하지 않는다.

#### FR-TIP-003 공개 팁 수정 `MUST`

- 작성자는 자신이 작성한 공개 팁을 수정할 수 있어야 한다.
- 수정 내용은 저장 즉시 공개 화면에 반영해야 한다.
- 다른 사용자의 공개 팁은 수정할 수 없어야 한다.

#### FR-TIP-004 공개 취소·재공개 `MUST`

- 작성자가 공개를 취소하면 팁 상태를 `UNPUBLISHED`로 변경한다.
- `UNPUBLISHED` 팁은 다른 사용자에게 표시하지 않는다.
- 작성자가 다시 공개하면 동일 팁을 `PUBLISHED`로 변경한다.
- 재공개 시 팁 ID와 작성일을 유지한다.

#### FR-TIP-005 공개 팁 삭제 `MUST`

- 작성자는 자신의 공개 팁을 삭제할 수 있어야 한다.
- 삭제한 팁의 상태는 `DELETED`로 변경한다.
- `DELETED` 팁은 다시 공개할 수 없어야 한다.

#### FR-TIP-006 공개 팁 신고 `MUST`

- 로그인 사용자는 다른 사용자의 공개 팁을 신고할 수 있어야 한다.
- 신고자는 신고 사유를 선택할 수 있어야 한다.
- 동일 사용자는 동일 팁을 한 번만 신고할 수 있어야 한다.
- 신고가 5회 누적되면 팁 상태를 자동으로 `HIDDEN`으로 변경한다.

#### FR-TIP-007 공개 팁 운영 처리 `MUST`

- 운영자는 신고된 팁 목록을 조회할 수 있어야 한다.
- 운영자는 신고 횟수와 사유를 확인할 수 있어야 한다.
- 운영자는 팁을 `HIDDEN`, `PUBLISHED`, `DELETED`로 변경할 수 있어야 한다.
- 운영자 기능은 `ADMIN` 권한으로 제한한다.

#### FR-TIP-008 탈퇴 후 공개 팁 보존 `MUST`

- 공개 팁은 공개 시점의 레시피 제목·원본 URL·내용을 별도로 보존해야 한다.
- 원본 `RecipeLink`가 삭제되어도 공개 팁은 익명 콘텐츠로 조회할 수 있어야 한다.
- 회원 탈퇴 시 공개 팁의 `author_user_id`는 `NULL`이 되어야 한다.

### 6.6 검색과 다시 찾기

#### FR-SEARCH-001 기본 검색 `MUST`

- 사용자는 메뉴명과 레시피 제목으로 자신의 레시피를 검색할 수 있어야 한다.
- 검색 결과에는 개인 소유 레시피만 포함해야 한다.

#### FR-SEARCH-002 필터와 정렬 `SHOULD`

- 플랫폼 필터를 제공한다.
- 별점 필터를 제공한다.
- 최근 저장순과 최근 조리 기록순 정렬을 제공한다.

#### FR-SEARCH-003 자연어 검색 `COULD`

- RAG 기반 자연어 검색은 MVP에서 구현하지 않는다.

### 6.7 PWA 공유 수신

#### FR-SHARE-001 공유 데이터 수신 `MUST`

- Android 설치형 PWA는 `title`, `text`, `url` 공유 데이터를 받을 수 있어야 한다.
- 공유 수신 후 자동 저장하지 않고 메뉴 확인 화면을 표시해야 한다.
- URL이 없는 공유 요청은 사용자에게 URL 입력을 요청해야 한다.

#### FR-SHARE-002 인증 전 공유 보존 `MUST`

- 로그인하지 않은 상태의 공유 데이터는 임시 보존할 수 있어야 한다.
- 로그인 완료 후 저장 확인 화면에서 공유 데이터를 복원해야 한다.

#### FR-SHARE-003 플랫폼 범위 `MUST`

- 1차 검증 대상은 Android 설치형 PWA다.
- iOS 네이티브 공유 확장은 MVP에서 제공하지 않는다.

## 7. 데이터 요구사항

### 7.1 핵심 엔터티

- `User`: 계정과 역할
- `AuthIdentity`: 로컬 또는 외부 인증 수단과 사용자 계정의 연결
- `Menu`: 사용자별 메뉴
- `RecipeLink`: 사용자별 저장 레시피
- `CookLog`: 개인 조리 기록
- `PublicTip`: 레시피에 연결된 익명 공개 팁
- `PublicTipReport`: 팁 신고와 신고자 중복 방지
- `PublicTipModerationLog`: 운영자 처리 이력

### 7.2 데이터 소유권

`PublicTipModerationLog`는 다음 정보를 보존한다.

- public_tip_id
- admin_user_id
- previous_status
- new_status
- reason
- created_at

- `User`, `Menu`, `RecipeLink`, `CookLog`는 사용자 소유 데이터다.
- 일반 사용자는 자신의 소유 데이터만 조회·수정·삭제할 수 있다.
- `PublicTip`은 작성자 내부 식별자를 가지지만 외부에는 익명 콘텐츠로 노출한다.
- `PublicTip`은 원본 개인 기록과 생명주기를 분리한다.

### 7.3 공개 팁 상태 전이

```text
PUBLISHED ── 공개 취소 ──> UNPUBLISHED ── 재공개 ──> PUBLISHED
    │
    ├── 신고 5회 누적 ──> HIDDEN ── 운영자 해제 ──> PUBLISHED
    │
    └── 작성자 삭제 ──> DELETED
```

- `UNPUBLISHED`는 작성자가 다시 공개할 수 있다.
- `HIDDEN`은 운영자가 검토 후 해제할 수 있다.
- `DELETED`는 복구하지 않는다.

### 7.4 데이터 보존

- 공개 팁은 공개 당시 레시피 제목·원본 URL·내용을 스냅샷으로 보존한다.
- 원본 레시피 삭제 시 공개 팁의 원본 연결만 해제한다.
- 회원 탈퇴 시 공개 팁의 작성자 연결만 제거한다.
- 정확한 개인정보 보존 기간과 백업 삭제 주기는 별도 운영 정책에서 정의한다.

## 8. 보안·비기능 요구사항

### 8.1 인증·권한

- 비밀번호는 최소 8자, 최대 72자다. 대문자·소문자·숫자·특수문자 혼용은 강제하지 않는다.
- 비밀번호 유출 데이터베이스 연동 검사는 MVP에서 제공하지 않는다.
- 비밀번호는 BCrypt 또는 동등한 안전한 해시 방식으로 저장한다.
- JWT Access Token은 15분, Refresh Token은 7일 동안 유효하다.
- Access Token과 Refresh Token은 HttpOnly·Secure Cookie로 전달한다.
- Refresh Token은 해시하여 PostgreSQL에 저장하고 Rotation한다.
- 이미 사용된 Refresh Token이 다시 사용되면 해당 토큰 계열을 폐기한다.
- 로그아웃·회원 탈퇴 시 Refresh Token을 폐기한다.
- Access Token Cookie는 `HttpOnly`, `Secure`, `SameSite=Lax`, `Path=/`를 사용한다.
- Refresh Token Cookie는 `HttpOnly`, `Secure`, `SameSite=Strict`, `Path=/api/v1/auth`를 사용한다.
- CSRF 방어, CORS 허용 출처 제한, Origin 검증을 적용한다.
- 인증 정보는 서버가 검증한다.
- 사용자 소유권 검증을 모든 개인 데이터 API에 적용한다.
- 관리자 API는 `ADMIN` 역할을 검증한다.
- 인증 실패·권한 없음·존재하지 않는 리소스를 일관된 오류 응답으로 처리한다.

### 8.2 URL 요청 보안

- 허용 스킴은 `http`, `https`로 제한한다.
- 메타데이터 요청에는 타임아웃을 둔다.
- 내부망·로컬 주소 접근을 차단한다.
- 응답 크기와 리다이렉트 횟수를 제한한다.
- 원본 콘텐츠를 다운로드하거나 재배포하지 않는다.

### 8.3 개인정보·익명성

- 개인 메모는 기본 비공개다.
- 공개 팁에는 이름·이메일·프로필·사용자 ID를 포함하지 않는다.
- 공개 팁 운영 처리를 위해 내부 작성자 식별자를 보관할 수 있다.
- 공개 전 개인정보 입력 주의 문구를 제공한다.

### 8.4 안정성·운영

- 동일 URL 저장은 멱등적으로 처리한다.
- 외부 메타데이터 추출 실패가 레시피 저장 전체 실패로 이어지지 않도록 한다.
- 공개 팁 신고와 운영 처리 이력을 확인할 수 있어야 한다.
- API 요청 크기와 URL 길이를 제한한다.

### 8.5 API 계약

- JSON 필드는 `camelCase`를 사용한다.
- 단일 리소스 성공 응답은 리소스 객체를 직접 반환한다.
- 목록 응답은 `items`, `page`, `size`, `totalElements`를 포함한다.
- API는 `200`, `201`, `204`, `400`, `401`, `403`, `404`, `409`, `422`, `500`을 목적에 맞게 사용한다.
- 오류 응답은 `code`, `message`, `details` 구조를 사용한다.

### 8.6 검색·페이지네이션

- 목록 기본 크기는 20, 최대 크기는 100이다.
- 페이지 번호는 0부터 시작한다.
- 검색어 앞뒤 공백을 제거한다.
- 메뉴명과 레시피 제목을 부분 일치로 검색한다.
- 검색은 대소문자를 구분하지 않는다.
- 허용되지 않은 정렬값은 오류로 처리한다.

### 8.7 신고·운영

- 신고 사유 코드는 `ABUSE`, `PRIVACY`, `SPAM`, `OFF_TOPIC`, `OTHER`다.
- 운영자 처리 이력은 별도 로그 데이터로 보존한다.

## 9. 기술·실행 요구사항

### 9.1 개발 환경

- Java 21
- Spring Boot
- Spring Security
- Spring Data JPA
- PostgreSQL
- Flyway
- Gradle
- JUnit 5
- Testcontainers
- Docker Compose

### 9.2 클라이언트

- Next.js
- TypeScript
- Tailwind CSS
- PWA manifest와 `share_target`

### 9.3 배포·자동화

- AWS Lightsail 단일 인스턴스
- Docker Compose 기반 실행
- HTTPS와 도메인
- Terraform 기반 인프라 관리
- GitHub Actions 기반 테스트·배포

### 9.4 AI 확장 원칙

- MVP 핵심 기능은 AI 없이 동작해야 한다.
- 초기 AI 연동은 Spring AI와 외부 LLM API를 우선한다.
- 이미지·음성·직접 모델 실험이 필요해질 때만 Python Worker를 별도로 검토한다.

## 10. 완료 기준

### 10.1 핵심 사용자 흐름

- 회원가입·로그인이 가능하다.
- URL을 저장하고 메뉴별로 분류할 수 있다.
- 저장한 레시피를 다시 열 수 있다.
- 조리 메모를 기본 비공개로 작성할 수 있다.
- 공개 체크 시 익명 팁이 즉시 표시된다.
- 레시피 상세에서 별도 익명 팁을 작성할 수 있다.
- 공개 팁을 수정·공개 취소·재공개·삭제할 수 있다.
- 공개 팁 신고 5회 누적 시 자동 숨김된다.
- 운영자가 신고 팁을 처리할 수 있다.
- 회원 탈퇴 후에도 공개 팁이 익명으로 유지된다.
- Android 설치형 PWA에서 공유 URL을 수신할 수 있다.

### 10.2 품질 기준

- 사용자 간 개인 데이터가 노출되지 않는다.
- 권한 없는 사용자가 관리자 API를 호출할 수 없다.
- 메타데이터 추출 실패에도 URL 저장이 가능하다.
- 중복 URL이 중복 생성되지 않는다.
- 기본 인증·권한·검증·오류 테스트가 통과한다.
- Testcontainers 기반 PostgreSQL 통합 테스트가 통과한다.

## 11. 결정사항 요약

MVP 기능 방향과 개발에 필요한 주요 정책은 확정했다. 구현 중 발생하는 세부 기술 선택은 기능명세서와 코드 리뷰에서 관리한다.
- 운영자 화면의 상세 레이아웃
- 신고 사유의 최종 표시 문구
- Terraform이 생성·관리할 AWS 리소스의 세부 목록

## 12. 개발 우선순위

### Phase 1: 개발 기반

- 저장소 구조와 환경 변수
- Docker Compose PostgreSQL
- Spring Boot 기본 설정
- Next.js 기본 화면
- Flyway 초기 마이그레이션
- 테스트 실행 환경

### Phase 2: 핵심 저장 흐름

- 회원가입·로그인
- 메뉴 CRUD
- URL 저장과 메타데이터 미리보기
- 레시피 목록·상세
- 중복·소유권 검증

### Phase 3: 조리 경험

- 조리 기록·개인 메모
- 익명 공개 체크박스
- 레시피 상세 익명 팁 작성·조회
- 공개 팁 수정·취소·재공개·삭제

### Phase 4: 운영과 재방문

- 공개 팁 신고
- 5회 누적 자동 숨김
- 운영자 화면
- 검색·필터·정렬

### Phase 5: 공유와 배포

- Android PWA 공유 수신
- Docker 이미지 빌드
- Terraform 기초 구성
- Lightsail 배포
- GitHub Actions 자동화
